# CORE pitfalls — Groovy (always injected)

The promoted, broadly-useful Groovy lessons — the can't-miss set surfaced into
every coder/debugger run. A fixed-size set (≤ 12): the background Librarian moves bullets
in from `Groovy.md` and demotes them back out to stay within the cap. Plugin/
environment-specific lessons are never kept here. Auto-maintained; do not edit by hand.
